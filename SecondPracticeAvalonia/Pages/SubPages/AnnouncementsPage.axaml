<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SecondPracticeAvalonia.Pages.SubPages.AnnouncementsPage">
    <Grid Margin="20" Background="#FCFCFC">
        <StackPanel Orientation="Vertical" Spacing="16">
            <TextBlock Text="📢 Управление анонсами" FontSize="20" FontWeight="Bold" Foreground="#2E7D32"/>
            
            <Grid ColumnDefinitions="Auto,*,Auto" Margin="0" RowDefinitions="Auto,Auto">
                <TextBlock Text="Выберите курс:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" Foreground="#546E7A"/>
                <ComboBox x:Name="CourseComboBox" Grid.Column="1" Margin="0,0,20,0" Background="#FFFFFF" BorderBrush="#E0E0E0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Button Grid.Column="2" Content="➕ Добавить анонс" Name="AddAnnouncementButton" Click="AddAnnouncement_Click" Background="#4CAF50" Foreground="White"/>
            </Grid>

            <DataGrid Name="AnnouncementsGrid" AutoGenerateColumns="False" IsReadOnly="True" Background="#FFFFFF" BorderThickness="1" BorderBrush="#E0E0E0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="📝 Название" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="📄 Содержание" Binding="{Binding Content}" Width="300"/>
                    <DataGridTextColumn Header="📅 Дата" Binding="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Width="150"/>
                    <DataGridCheckBoxColumn Header="✅ Опубликовано" Binding="{Binding IsPublished}" Width="100"/>
                    <DataGridTemplateColumn Header="⚙️ Действия" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Name="EditAnnouncement" Content="✏️ Редактировать" Tag="{Binding}" Click="EditAnnouncement_Click" Background="#2E7D32" Foreground="White" Padding="8,4"/>
                                    <Button Name="DeleteAnnouncement" Content="🗑️ Удалить" Tag="{Binding}" Click="DeleteAnnouncement_Click" Background="#E53935" Foreground="White" Padding="8,4"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
