<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SecondPracticeAvalonia.Pages.StudentPage">
  <DockPanel Background="#FCFCFC">
    <Border DockPanel.Dock="Top" Background="#2E7D32" Padding="16" Height="60">
      <Grid ColumnDefinitions="Auto,*,Auto,Auto" VerticalAlignment="Center">
        <TextBlock Text="👨‍🎓 Студент" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
        <StackPanel Grid.Column="1" Orientation="Horizontal"  HorizontalAlignment="Center">
          <Button Content="Мои курсы" Click="OnCoursesClick" Background="#2E7D32" Foreground="White" Height="35"/>
          <Button Content="Просмотр курсов" Click="OnBrowseCoursesClick" Background="#2E7D32" Foreground="White" Height="35"/>
          <Button Content="Обсуждения" Click="OnDiscussionsClick" Background="#2E7D32" Foreground="White" Height="35"/>
          <Button Content="Анонсы" Click="OnAnnouncementsClick" Background="#2E7D32" Foreground="White" Height="35"/>
          <Button Content="Прогресс" Click="OnProgressClick" Background="#2E7D32" Foreground="White" Height="35"/>
        </StackPanel>
        <TextBlock Grid.Column="2" x:Name="UserInfoText" Foreground="White" Margin="8,0" VerticalAlignment="Center" FontSize="14"/>
        <Button Grid.Column="3" Content="Выход" Click="OnLogoutClick" Background="#E53935" Foreground="White" Height="35"/>
      </Grid>
    </Border>

    <ScrollViewer>
      <Grid Margin="20" Background="#FCFCFC">
        <DataGrid x:Name="CoursesGrid" AutoGenerateColumns="False" IsReadOnly="True" Background="#FFFFFF" BorderThickness="1" BorderBrush="#E0E0E0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="📚 Курс" Binding="{Binding Title}" Width="350"/>
            <DataGridTextColumn Header="📂 Категория" Binding="{Binding CategoryName}"/>
            <DataGridTextColumn Header="📊 Прогресс %" Binding="{Binding Progress}"/>
            <DataGridTemplateColumn Header="⚙️ Действия" Width="150">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <Button Name="OpenCourse" Content="Открыть" Tag="{Binding CourseId}" Click="OpenCourse_Click" Background="#2E7D32" Foreground="White"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
          </DataGrid.Columns>
        </DataGrid>

        <DataGrid x:Name="ProgressGrid" AutoGenerateColumns="False" IsReadOnly="True" IsVisible="False" Background="#FFFFFF" BorderThickness="1" BorderBrush="#E0E0E0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="📖 Урок" Binding="{Binding LessonTitle}" Width="350"/>
            <DataGridTextColumn Header="📚 Курс" Binding="{Binding CourseTitle}"/>
            <DataGridCheckBoxColumn Header="✅ Завершен" Binding="{Binding IsCompleted}"/>
            <DataGridTextColumn Header="⏱️ Секунды" Binding="{Binding LastPositionSeconds}"/>
          </DataGrid.Columns>
        </DataGrid>

        <ContentControl x:Name="ContentArea" Background="#FCFCFC">
          <!-- SubPages will be loaded here -->
        </ContentControl>
      </Grid>
    </ScrollViewer>
  </DockPanel>
</UserControl>




